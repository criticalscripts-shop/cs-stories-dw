const config = {
    // Enter here the Discord webhook of the channel you want to post to.
    discordWebhookUrl: '',

    // Which identifier to post on Discord? If your server does not require Steam using something like 'license'.
    logIdentifier: 'steam',

    // Do not change unless you have renamed cs-stories to something else.
    resourceName: 'cs-stories'
}
